apiVersion: v1
kind: Service
metadata:
  name: cloudguard-mock-ai
  namespace: ai-security
spec:
  selector:
    app: cloudguard-mock-ai
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
